# ============================================================================
# GOOGLE MAPS/PLACES API - Already configured in Vercel
# ============================================================================
# This key is already set in your Vercel environment
# It works for both Google Maps AND Google Places API (New)
# No need to create a new key - use the existing one
GOOGLE_MAPS_API_KEY=AIzaSyA5xhbpf14ajrT1axR8iaMw9m2u_jpMGpE

# ============================================================================
# MICROSOFT GRAPH API - Required for email delivery
# ============================================================================
# Azure AD App Registration for Graph API sendMail
# Setup instructions:
# 1. Go to https://portal.azure.com
# 2. Navigate to Azure Active Directory → App registrations
# 3. Create new registration or use existing
# 4. Note the Application (client) ID and Directory (tenant) ID
# 5. Create a client secret under Certificates & secrets
# 6. Grant API permissions: Mail.Send (Application permission)
# 7. Admin consent required for Mail.Send permission
#
# This replaces SMTP and provides better Microsoft 365 integration
OAUTH_TENANT_ID=your_tenant_id_here
OAUTH_CLIENT_ID=your_client_id_here
OAUTH_CLIENT_SECRET=your_client_secret_here
